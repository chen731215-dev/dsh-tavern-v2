## v1.9.2 (2026-08-23)

### ✨ 新功能
- **可配置玩家名**：酒馆设置 → 记忆与总结面板新增「玩家名」输入框，填入后自动替换角色卡/世界书中的 {{user}} 占位符（如：栎木），并统一关系网中玩家节点，不再出现「你」与「玩家名」两个实体
- **玩家名全局生效**：配置存储于 tavern-state.json，所有绑定酒馆预设的会话一致生效；不填时退回原「用户」默认行为，不影响未使用 {{user}} 的角色卡

### 🐛 修复
- **{{user}} 占位符清空**：修复插件把 {{user}}/{{name}}/{{char}} 替换为空字符串导致玩家身份字段蒸发的 bug，现改为替换为配置的玩家名
- **关系网身份分裂**：修复「你」与「栎木」等玩家名被识别为两个独立节点的问题，玩家名正确归并为「你」节点
- **deployment:persona 报错**：清理世界书中残留 {{user}} 导致的 unknown prompt variable 报错
- **发布版崩溃**：修复 cordis.patch.yml / client.manager.bundle.js 中硬编码 @local/dsh-tavern 本地别名导致用户安装后 Cannot find package 崩溃的问题，统一为正规包名 dsh-tavern

---
# Changelog

## v1.9.0 (2026-08-23)

### ✨ 新功能
- **状态栏美化**：AI 回复中的「状态栏：」格式（日期时间/地点/用户列表）自动渲染为紫色状态卡片
- **新建空白预设**：酒馆管理「＋ 新建」默认创建完全空白预设（不再复制上一个预设的角色卡/世界书）
- **复制当前预设**：新增「⧉ 复制」按钮，可完整复制当前预设

### 🐛 修复
- **会话错位**：美化只作用于当前可见会话，不再渲染到其他会话的消息上
- **<content> 标签泄漏**：AI 输出里的 <content> 草稿容器标签自动剥离，不再显示在正文
- **新建预设默认选中**：新建/复制预设后自动切换到新预设，不再停留在旧预设

---



## v1.7.1 (2026-08-20)

### 📝 文档与合规
- 新增「写 NSFW 的正确用法」：酒馆预设 + 已配置好的 agent 预设必须搭配一起使用（README / TUTORIAL）
- 移除第三方预设引用（类脑社区「三人逆行」），示例改为通用说法，避免来源问题
- 修正预设示例名，改为中性示例
- 内置 `tests/core.test.js`（`lib/utils.js` 单元测试，17 个用例，`node --test tests\` 可跑）
- package.json 补充 repository 字段

## v1.7.0 (2026-08-19)

### ✨ 新功能
- **预设直接注入**：酒馆预设直接注入系统提示，AI 不翻硬盘也能读到完整角色卡/世界书/预设
- **Agent 预设同名同步**：新建/重命名/保存酒馆预设时，自动生成同名 DSH Agent 预设，可直接在顶部切换
- **Agent 预设管理**：在酒馆管理里集中管理所有 DSH Agent 预设，支持搜索、分组折叠、批量删除
- **来源识别**：自动区分「🍺 酒馆预设（插件生成）」「🛡️ DSH 自带」「🧩 其他/自定义」
- **世界书统一 v2 格式**：只保留一种持久化结构，旧数据自动迁移，避免重复/覆盖问题
- **重复导入检测**：世界书/角色卡/预设导入时自动检测同名并询问合并或替换
- **一键切换 Agent 预设**：在酒馆管理里点击「⚡ 切到Agent预设」自动切换到同名 Agent 预设
- **工具规则**：默认不翻硬盘/不搜历史，用户明确要求时才允许使用文件工具，防止乱搞

### 🐛 修复
- 修复世界书“全文注入”模式没有真正全量注入的问题
- 修复旧世界书数组/对象两种格式互相覆盖的问题
- 修复 Agent 预设名一直叫“精简酒馆”，不跟随酒馆预设名的问题
- 修复默认酒馆预设与 DSH 自带“默认预设”重名/重合问题（改为「酒馆默认」）

### 🎨 优化
- 两个批量删除入口合并为一个统一的 Agent 预设管理
- Agent 预设列表支持按来源分组折叠
- 内置 DSH 预设保护，防止误删系统预设

## v1.6.0 (2026-08-19)

### ✨ 新功能
- **世界书智能注入**：全局条目每次注入，角色条目按关键词触发，节省 60-70% 上下文
- **世界书卡片式 UI**：按世界书分组折叠，条码+展开详情，条目名称和关键词正确显示
- **剧情选项独立开关**：不依赖成人模式，可单独开启
- **剧情选项交互**：点击选项自动填入输入框并发送（React 兼容）
- **角色卡导入同步世界书**：导入角色卡时自动提取并同步自带世界书
- **跨工作区会话过滤**：故事背景只显示当前工作区的会话
- **强制角色卡覆盖**：最高优先级指令，忽略其他插件注入的角色设定，解决串台问题
- **注入顺序调整**：order 改为 999，确保在所有插件之后注入

### 🐛 修复
- 修复世界书管理区域和简单导入区域 ID 冲突导致点击没反应
- 修复世界书条目名称显示"未命名"（用 comment 而非 name）
- 修复世界书关键词不显示（用 keys 而非 keywords）
- 修复小词条不能展开（分组条目引用问题）
- 修复"全部展开/折叠"按钮引用局部变量导致报错
- 修复选择文件按钮事件冒泡导致重复触发
- 修复删除世界书管理区域后面板空白（事件绑定引用已删除元素）
- 修复剧情选项重复显示（原始文本和按钮同时出现）
- 修复剧情选项点击后输入框内容瞬间消失（React 状态覆盖）
- 修复角色卡串台（其他记忆/召回插件注入的角色设定覆盖酒馆角色卡）
- 修复手动总结不同步关系网（强化关系提取提示词）
- 修复预设切换后角色卡/世界书丢失

### 🎨 优化
- 合并两个世界书区域为一个，注入模式、新增条目、拖放导入集成在一起
- 条目内容文本域从 3 行增至 8 行，等宽字体
- 世界书拖放区域 data-type 修复
- 注入调试日志优化，显示世界书智能注入统计
- CARD_MAX 提升至 120000 字符

### 🔞 破限增强
- 集成 session-seed-plugin 历史共识破限法
- 成人模式注入新增【历史共识】和【关键记忆】段落
- NSFW 开关联动成人模式

## v1.5.0 (2026-08-18)

### ✨ 新功能
- 多预设管理 + 会话级预设绑定
- 白名单模式，只有加入白名单的会话才注入
- 预设删除、批量删除、全选
- 记忆模块（手动/自动总结）
- 角色关系网
- NSFW 写作模式
- AI 回复编辑功能
- AI 回复美化（剧情选项渲染）

### 🐛 修复
- 修复预设切换后配置丢失
- 修复注入逻辑读取错误文件
- 修复 UI 布局错位

## v1.0.0 (2026-08-17)

### ✨ 初始版本
- 角色卡导入/管理
- 世界书导入/管理
- 预设导入/切换
- 基础注入功能
